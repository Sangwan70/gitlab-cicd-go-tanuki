package main

/* Solution: Add new Version function - tests */
/*
import (
    "testing"
    "github.com/blang/semver/v4"
)

func TestGetVersion(t *testing.T) {
    _, err := semver.Parse(GetVersion())

    if err != nil {
        t.Errorf("GetVersion failed at checking for semantic version.")
    }
}
*/
